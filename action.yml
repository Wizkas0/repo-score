  
name: 'ScoreBored'
description: 'Generate labels for pull requests'
branding:
  icon: 'check-circle'  
  color: 'red'
inputs:
  repo-token:
      description: 'The GITHUB_TOKEN repo secret'
  something:
      description: 'something'
runs:
  using: 'node12'
  main: 'index.js'
